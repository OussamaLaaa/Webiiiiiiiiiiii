# Dashboard Password (for API authentication)
# Change this to a secure password in production
DASHBOARD_PASSWORD=00000008

# API URL (optional, defaults to /api)
# NEXT_PUBLIC_API_URL=https://your-domain.com/api